cd /c/Users/HP/io/imf-replizierung

# 1. Git-Repository initialisieren (falls noch nicht geschehen)
git init

# 2. Alle Dateien hinzufügen (außer .gitignore)
git add .

# 3. Status prüfen (sollte keine xlsx/dta-Dateien anzeigen)
git status

# 4. Commit erstellen
git commit -m "Datenstruktur für IMF-Replikation aufgestellt
- Originalskripte in code/original/ abgelegt
- Rohdaten in data/raw/ strukturiert
- Verarbeitete Daten in data/processed/
- GitHub-Synchronisation vorbereitet"

Generated by Mistral Vibe.
Co-Authored-By: Mistral Vibe <vibe@mistral.ai>"

# 5. Remote-Repository hinzufügen (falls noch nicht geschehen)
git remote add origin https://github.com/IHR_BENUTZERNAME/imf-replizierung.git

# 6. Pushen
git push -u origin main